<%= form_for(@results_dictionary) do |f| %>
  <% if @results_dictionary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@results_dictionary.errors.count, "error") %> prohibited this results_dictionary from being saved:</h2>

      <ul>
      <% @results_dictionary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
