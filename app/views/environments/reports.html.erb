<div class="modal fade" id="cases" title="My modal">
  <div class="modal-header">
    
  </div>
  <div class="modal-body">
  	<button aria-hidden="true" class="close" id="health-modal-close" type="button">×</button>
  	<div id="health-modal">
    
    </div>
  </div>
  <div class="modal-footer">
  </div>
</div>
<h1 style="text-align:center;">Test Suites for <%= "#{@environment_name}" %></h1>
<form action="/run_suites" method="post">
	<input name="authenticity_token" value="<%= form_authenticity_token %>" type="hidden">
	<table id="suite-table-dude" style="width:100%">
		<thead>
			<tr>
				<th>Schedule Id</th>
				<th>Testcase Name</th>
				<th></th>
				
			</tr>
		</thead>
		<!-- <tbody>
		  <% @test_suites.each do |ts| %>
			<tr>
				<td><%= ts.id %></td>
				<td><%= ts.name %></td>
				<td class="button-cell"><%#= link_to "List all the reports", {controller: "test_suites", action: "test_cases", id: ts.id }, "data-toggle" => "modal", "data-target" => "#cases", class: "btn btn-default btn-sm", remote: true %>
					<%#= link_to "Edit", edit_test_suite_path(ts.id), class: "btn btn-default btn-sm" %>
					<%#= link_to "Schedule", {controller: "test_suites", action: "tests_ran", id: ts.id }, class: "btn btn-default btn-sm", "data-toggle" => "modal", "data-target" => "#cases", remote: true %>
				</td>
			</tr>
		  <% end %>
		</tbody> -->
		<tbody>
		  <% @schedule.each do |s| %>
			<tr>
				<td><%= s.id %></td>
				<td><%= TestSuite.find(s.test_suite_id).name %></td>
				<td class="button-cell"><%#= link_to "List all the reports", {controller: "test_suites", action: "schedule_cases", id: s.id }, "data-toggle" => "modal", "data-target" => "#cases", class: "btn btn-default btn-sm", remote: true %>
				<%= link_to "List All Reports","list_all_reports", target: :_blank %>
				</td>
			</tr>
		  <% end %>
		</tbody>
	</table>
	
</form>

<style>
table, th, td {
  border: 1px solid black;
  border-collapse: collapse;
}
</style>

<script>
	$("#suite-table-dude").dataTable();
</script>

